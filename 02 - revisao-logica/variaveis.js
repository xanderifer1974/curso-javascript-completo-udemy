let minhaVariavel = "teste de conteudo";
minhaVariavel = 10; // O valor pode ser alterado, mas não podemos declarar a mesma variável mais de uma vez.
console.log(minhaVariavel);

const user = 'xanderifer';

// user = 'Luciana'; Não pode ser atribuido valor

console.log(user);